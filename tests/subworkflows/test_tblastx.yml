- name: tblastx-bactopia_help
  command: bash -c 'nextflow run ./main.nf --wf tblastx -c nextflow.config --help > tblastx-help.txt'
  tags:
    - bactopia_help
    - tblastx
  files:
    - path: tblastx-help.txt
      contains: ['tblastx', 'bactopia', 'version', 'dependencies']
