/*
This file includes default values for shared parameters.
*/

params {
    is_subworkflow = false

    // Generic Parameters
    available_datasets = null

    // Parameters to ignore
    empty_r1 = "${baseDir}/data/EMPTY_R1"
    empty_r2 = "${baseDir}/data/EMPTY_R2"
    empty_extra = "${baseDir}/data/EMPTY_EXTRA"
    empty_proteins = "${baseDir}/data/EMPTY_PROTEINS"
    empty_tf = "${baseDir}/data/EMPTY_TF"
    empty_adapters = "${baseDir}/data/EMPTY_ADAPTERS"
    empty_phix = "${baseDir}/data/EMPTY_PHIX"

    // Input Parameters
    samples = null
    check_samples = false
    R1 = null
    R2 = null
    SE = null
    sample = null
    ont = false
    hybrid = false
    short_polish = false
    accessions = null
    accession = null
    assembly = null
    reassemble = false

    // Dataset Parameters
    datasets = null
    species = null
    ask_merlin = false

    // Optional Parameters
    coverage = 100
    genome_size = "0"

    // Max Job Request Parameters
    max_retry = 3
    min_time = 60
    max_time = 120
    max_memory = 32
    max_cpus = 4
}
