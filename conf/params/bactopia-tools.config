/*
This file includes default values for shared parameters.
*/

params {
    // Parameters to ignore
    empty_path = "${projectDir}/../../../data/empty"

    // Bactopia Tools
    include = null
    exclude = null

    // Mix downloads with local samples
    mix_downloads = true
}
