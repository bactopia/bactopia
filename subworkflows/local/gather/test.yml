- name: gather-test
  command: bash -c 'nextflow run ./subworkflows/local/gather/test.nf -entry test_gather --wf gather -c nextflow.config ${BACTOPIA_ARGS}'
  tags:
    - gather
    - bactopia-tools
  files:
    - path: OUTPUTFILE
      contains: ['TEXT_A', 'TEXT_B']
    - path: OUTPUTFILE
      md5sum: a1000000000000000000000000000000
