- name: assembler-test
  command: bash -c 'nextflow run ./subworkflows/local/assembler/test.nf -entry test_assembler --wf assembler -c nextflow.config ${BACTOPIA_ARGS}'
  tags:
    - assembler
    - bactopia-tools
  files:
    - path: OUTPUTFILE
      contains: ['TEXT_A', 'TEXT_B']
    - path: OUTPUTFILE
      md5sum: a1000000000000000000000000000000
