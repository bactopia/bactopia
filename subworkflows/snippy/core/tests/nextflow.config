// Minimal config for subworkflow-level testing of SNIPPY_CORE
nextflow.enable.types = true
nextflow.enable.strict = true
includeConfig "../../../../conf/test_base.config"

params {
    workflow {
        name = "snippy-core"
        logo_name = "bactopia-tools"
        description = "Core SNP alignment from Snippy outputs"
        ext = "aln"
    }
    wf = params.workflow.name

    // Module-specific defaults
    skip_compression = false
}

// Load module configs for processes in this subworkflow
includeConfig "../../../../modules/snippy/core/module.config"
includeConfig "../../../../modules/snpdists/module.config"

// Base config (container resolution + resource labels)
includeConfig "../../../../conf/base.config"
includeConfig "../../../../conf/profiles.config"
