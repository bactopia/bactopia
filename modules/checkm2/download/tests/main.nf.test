nextflow_process {
    name "Test CHECKM2_DOWNLOAD"
    script "../main.nf"
    process "CHECKM2_DOWNLOAD"
    tag "modules"
    tag "checkm2"
    tag "checkm2_download"

    // NOTE: This module requires internet access to download databases.
    // Tests are stubs - run manually with: nf-test test tests/main.nf.test
    test("checkm2/download - module - download") {

        when {
            params {
                test_data_dir = System.getenv("BACTOPIA_TESTS") ?: ""
            }
            process {
                """
                // No inputs required - download module
                """
            }
        }

        then {
            assertAll(
                { assert process.success }
            )
        }
    }
}
