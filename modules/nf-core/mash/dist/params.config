/*
This file includes default parameter values.
*/

params {
    // Mash (dist)
    mash_sketch = null
    mash_seed = 42
    mash_table = false
    mash_m = 1
    mash_w = 0.01
    max_p = 1.0
    max_dist = 1.0 // report all
    merlin_dist = 0.10 // report only close matches
    full_merlin = false
    merlin_db = "https://datasets.bactopia.com/datasets/v${manifest.version}/merlin-refseq-k21.msh.gz"
}
