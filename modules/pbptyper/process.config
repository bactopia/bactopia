process {
    withName: 'PBPTYPER' {
        // Optional arguments
        ext.args = {[
            "--min_pident ${params.pbptyper_min_pident}",
            "--min_coverage ${params.pbptyper_min_coverage}"
        ].join(' ').trim()}
        
        // Environment information
        ext.conda = "bioconda::pbptyper=2.0.0"
        ext.singularity = "https://depot.galaxyproject.org/singularity/pbptyper:2.0.0--hdfd78af_0"
        ext.docker = "quay.io/biocontainers/pbptyper:2.0.0--hdfd78af_0"
        
        ext.prefix = { params.prefix ? params.prefix : "${params.wf == 'bactopia' ? meta.id : meta.name}" }
        ext.rundir = "${params.rundir}"
        ext.subdir = ""
        ext.logs_subdir = ""
        ext.process_name = params.is_workflow ? params.wf : params.process_name
    }
}
