/*
Bactopia Module Configuration

Defines parameter defaults, the container images, resource labels, and other settings
for the process defined in this directory.
*/

params {
    // ngmaster
    ngmaster_csv = false
}

process {
    withName: 'NGMASTER' {
        // Optional arguments
        ext.args = [
            params.ngmaster_csv ? "--csv" : ""
        ].join(' ').replaceAll("\\s{2,}", " ").trim()
        
        // Environment information
        ext.toolName = "bioconda::ngmaster=1.1.1".replace("=", "-").replace(":", "-").replace(" ", "-")
        ext.docker = "biocontainers/ngmaster:1.1.1--pyhdfd78af_1"
        ext.image = "https://depot.galaxyproject.org/singularity/ngmaster:1.1.1--pyhdfd78af_1"
        ext.condaDir = "${params.condadir}"
        
        ext.wf = params.wf
        ext.scope = "sample"
        ext.subdir = ""
        ext.logs_subdir = ""
        ext.process_name = "ngmaster"
    }
}
