nextflow_process {
    name "Test SRAHUMANSCRUBBER_INITDB"
    script "../main.nf"
    process "SRAHUMANSCRUBBER_INITDB"
    tag "modules"
    tag "srahumanscrubber"
    tag "srahumanscrubber_initdb"

    // NOTE: This module requires internet access to download databases.
    // Tests are stubs - run manually with: nf-test test tests/main.nf.test
    test("srahumanscrubber/initdb - module - download") {

        when {
            params {
                test_data_dir = System.getenv("BACTOPIA_TESTS") ?: ""
            }
            process {
                """
                // No inputs required - download module
                """
            }
        }

        then {
            assertAll(
                { assert process.success }
            )
        }
    }
}
