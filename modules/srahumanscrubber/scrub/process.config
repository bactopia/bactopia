/*
This file includes default process configurations
*/

process {
    withName: 'SRAHUMANSCRUBBER_SCRUB' {
        // Optional arguments
        
        // Environment information
        ext.conda = "${moduleDir}/environment.yml"
        ext.singularity = 'https://depot.galaxyproject.org/singularity/bactopia-teton:1.1.1--hdfd78af_0'
        ext.docker = 'quay.io/biocontainers/bactopia-teton:1.1.1--hdfd78af_0'

        // Misc
        ext.prefix = { "${meta.id}" }
        ext.rundir = "${task.process}"
        ext.subdir = ""
        ext.logs_subdir = ""
        ext.process_name = { "${task.process}".replace(":", "_") }
    }
}
