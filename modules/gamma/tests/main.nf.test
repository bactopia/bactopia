nextflow_process {
    name "Test GAMMA"
    script "../main.nf"
    process "GAMMA"

    test("gamma - module - GCF_000292685") {
        when {
            params {
                test_data_dir = System.getenv("BACTOPIA_TESTS") ?: ""
            }
            process {
                """
                input[0] = Channel.of(
                    record(
                        _meta: [name: "GCF_000292685"],
                        assembly: file("${params.test_data_dir}/data/species/portiera/genome/GCF_000292685.fna.gz")
                    )
                )
                input[1] = file("${params.test_data_dir}/data/species/portiera/genome/GCF_000292685.ffn")
                """
            }
        }

        then {
            def record = process.out[0][0]
            assertAll(
                { assert process.success },
                { assert snapshot(
                    record.meta,
                    record.gamma,
                    record.psl,
                    record.versions
                ).match() }
            )
        }
    }
}
