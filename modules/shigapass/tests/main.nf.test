nextflow_process {
    name "Test SHIGAPASS"
    script "../main.nf"
    process "SHIGAPASS"
    tag "modules"
    tag "shigapass"

    test("shigapass - module - GCF_016726285 - uncompressed") {
        when {
            params {
                test_data_dir = System.getenv("BACTOPIA_TESTS") ?: ""
            }
            process {
                """
                input[0] = Channel.of(
                    record(
                        _meta: [name: "GCF_016726285"],
                        fna: file("${params.test_data_dir}/species/shigella_boydii/uncompressed/GCF_016726285/main/assembler/GCF_016726285.fna")
                    )
                )
                """
            }
        }

        then {
            def record = process.out[0][0]
            assertAll(
                { assert process.success },
                { assert snapshot(
                    record.meta,
                    record.tsv,
                    record.flex_tsv,
                    record.versions
                ).match() }
            )
        }
    }

    test("shigapass - module - GCF_016726285 - compressed") {
        when {
            params {
                test_data_dir = System.getenv("BACTOPIA_TESTS") ?: ""
            }
            process {
                """
                input[0] = Channel.of(
                    record(
                        _meta: [name: "GCF_016726285"],
                        fna: file("${params.test_data_dir}/species/shigella_boydii/compressed/GCF_016726285/main/assembler/GCF_016726285.fna.gz")
                    )
                )
                """
            }
        }

        then {
            def record = process.out[0][0]
            assertAll(
                { assert process.success },
                { assert snapshot(
                    record.meta,
                    record.tsv,
                    record.flex_tsv,
                    record.versions
                ).match() }
            )
        }
    }
}
